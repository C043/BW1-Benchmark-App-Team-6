/*
const checkBox = document.getElementsByClassName("inp")

checkBox.addEventListener("click", checkBox() => {
    checkBox.classList.add("checked")
})
checkBox()


const button = document.getElementById("btn")

button.addEventListener("click", () => {
    if () {
        const url = "./question.html"
        window.location.href = url
    } else {

    }
}
)*/

// 1. checkbox required prima di premere il proceed
// 2. "if" : è stato cliccato il checkBox 